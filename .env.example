# Database URL for Diesel CLI
DATABASE_URL=postgres://postgres:password@localhost/hserver_db

# Application Configuration (Overrides default.toml)
APP__DATABASE__URL=postgres://postgres:password@localhost/hserver_db
APP__SERVER__PORT=8080
APP__SERVER__HOST=127.0.0.1
